//! API types generated by Anti-Gravital ag-dsl v0.2.
//! DO NOT edit manually. Regenerate with `ag generate`.

#![allow(dead_code, missing_docs)]

use serde::{Deserialize, Serialize};

/// Request body for `CreateUserRequest`.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreateUserRequest {
    pub email: String,
    pub name: String,
}

impl CreateUserRequest {
/// Validates every constraint declared in the DSL schema.
/// Returns all validation errors; an empty list means the value is valid.
pub fn validate(&self) -> Vec<String> {
let mut errors: Vec<String> = Vec::new();
    if !(self.email.contains('@') && self.email.rfind('@')
.map(|i| self.email[i + 1..].contains('.'))
.unwrap_or(false)) {
        errors.push("email: invalid email format".to_owned());
    }
    if self.email.len() > 255 {
        errors.push(format!("email: maximum length is 255, found {} characters", self.email.len()));
    }
    if self.email.len() < 5 {
        errors.push(format!("email: minimum length is 5, found {} characters", self.email.len()));
    }
    if self.name.len() < 2 {
        errors.push(format!("name: minimum length is 2, found {} characters", self.name.len()));
    }
    if self.name.len() > 100 {
        errors.push(format!("name: maximum length is 100, found {} characters", self.name.len()));
    }
    errors
}
}

/// Request body for `CreateProductRequest`.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreateProductRequest {
    pub name: String,
    pub price: rust_decimal::Decimal,
    pub stock: i64,
    pub category_id: uuid::Uuid,
}

impl CreateProductRequest {
/// Validates every constraint declared in the DSL schema.
/// Returns all validation errors; an empty list means the value is valid.
pub fn validate(&self) -> Vec<String> {
let mut errors: Vec<String> = Vec::new();
    if self.name.len() < 2 {
        errors.push(format!("name: minimum length is 2, found {} characters", self.name.len()));
    }
    if self.name.len() > 200 {
        errors.push(format!("name: maximum length is 200, found {} characters", self.name.len()));
    }
    if self.price < rust_decimal::Decimal::from(0) {
        errors.push("price: minimum value is 0".to_owned());
    }
    if self.stock < 0 {
        errors.push("stock: minimum value is 0".to_owned());
    }
    errors
}
}

/// Request body for `CreateOrderRequest`.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreateOrderRequest {
    pub user_id: uuid::Uuid,
    pub items: String,
}

/// Response body for `UserResponse`.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UserResponse {
    pub id: uuid::Uuid,
    pub email: String,
    pub name: String,
}

/// Response body for `ProductResponse`.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ProductResponse {
    pub id: uuid::Uuid,
    pub name: String,
    pub price: rust_decimal::Decimal,
    pub stock: i64,
    pub category_id: uuid::Uuid,
}

/// Response body for `OrderResponse`.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct OrderResponse {
    pub id: uuid::Uuid,
    pub user_id: uuid::Uuid,
    pub total: rust_decimal::Decimal,
    pub status: String,
    pub created_at: uuid::Uuid,
}

/// HTTP error 404: Recurso no encontrado
pub const NOT_FOUND_STATUS: u16 = 404;
pub const NOT_FOUND_MESSAGE: &str = "Recurso no encontrado";

/// HTTP error 409: El email ya esta registrado
pub const EMAIL_TAKEN_STATUS: u16 = 409;
pub const EMAIL_TAKEN_MESSAGE: &str = "El email ya esta registrado";

/// HTTP error 422: Stock insuficiente para completar el pedido
pub const INSUFFICIENT_STOCK_STATUS: u16 = 422;
pub const INSUFFICIENT_STOCK_MESSAGE: &str = "Stock insuficiente para completar el pedido";

/// HTTP error 400: Datos de entrada invalidos
pub const INVALID_INPUT_STATUS: u16 = 400;
pub const INVALID_INPUT_MESSAGE: &str = "Datos de entrada invalidos";

